#include <stdlib.h>
#include <string.h>
#include <stdio.h>
#include "lists.h"

/**
 * find_listint_loop - function that prints a list
 * @head: first param
 *
 * Return: the number of nodes in the list
 */

listint_t *find_listint_loop(listint_t *head)
{
	return (head);
}
